//
//  NetworkService.swift
//  Millionaire
//
//  Created by Maksim Romanov on 14.02.2020.
//  Copyright © 2020 Maksim Romanov. All rights reserved.
//

import Foundation
import Alamofire

class NetworkService {
    /*
    static let manager: Session = {
        let config = URLSessionConfiguration.default
        config.timeoutIntervalForRequest = 30
        let manager = Session(configuration: config)
        return manager
    }()
    
    static func loadQuestion() {
        let baseUrl = "https://lip2.xyz/api/millionaire.php"
        
        let params: Parameters = [
            "q": 1
        ]
        
        NetworkService.manager.request(baseUrl, method: .get, parameters: params).responseJSON { response in
            switch response.result {
            case let .success(data):
                print(data)
            case let .failure(error):
                print(error)
            }
        }
    }*/
}
